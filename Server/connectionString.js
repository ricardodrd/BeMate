var connectionString = {
    connection: {
        admin: {
            host: "localhost",
            user: "root",
            password: "password",
            database: "world"
        }
    }
};

module.exports.connectionString = connectionString;